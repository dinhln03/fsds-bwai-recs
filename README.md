# How to guide

1. Convert dataset to CSV
    ```bash
    uv run python -m src.scripts.convert_dataset_to_csv
    ```
2. Compute item metadata
    ```bash
    uv run python -m src.scripts.compute_item_metadata
    ```